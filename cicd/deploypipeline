node {
  stage('test'){
    sh "echo inside and ls -ltr"
  }

  stage('Deploying into Openshift') {
              sh "oc apply -f deployment.yaml -n ronyvarghese3-dev "
            }
  }

  if (currentBuild.currentResult == 'SUCCESS') { 
      stage('Deploy') { 
        sh './deployscript'  
      } 
  } 
}
